apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: {{ include "account-app-chart.fullname" . }}
spec:
  rules:
    - host: arch.homework
      http:
        paths:
          - backend:
              serviceName: {{ include "account-app-chart.fullname" . }}
              servicePort: {{ .Values.service.port }}
            path: /account/register